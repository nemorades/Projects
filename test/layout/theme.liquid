<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ shop.name }} | Radiant Facial Cosmetics</title>
    <meta name="description" content="Luxurious, customizable facial cosmetic care with gentle formulas and glowing results.">
    {{ content_for_header }}
    {{ 'theme.css' | asset_url | stylesheet_tag }}
  </head>
  <body>
    <main id="MainContent" role="main">{{ content_for_layout }}</main>
    {{ 'theme.js' | asset_url | script_tag }}
  </body>
</html>
